[phases.setup]
nixPkgs = ['nodejs_22', 'python3']

[phases.install]
cmds = [
  'npm ci --prefer-offline --no-audit --progress=false'
]

[phases.build]
cmds = [
  'npm run build'
]

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
NPM_CONFIG_PRODUCTION = 'false'
